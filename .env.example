# Copy this file to .env and fill in your own local database credentials.
# .env is git-ignored, so your real credentials are never committed.

DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=customer_behavior
